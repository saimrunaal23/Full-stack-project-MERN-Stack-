extends layout

block content
  .main.container.text-xs-center
      h1.display-3 Welcome #{user.firstname}
      a.btn.btn-info-outline.btn-lg(href='/profile') Get Loans
      a.btn.btn-info-outline.btn-lg(href='/loan') Create Loans